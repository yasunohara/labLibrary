# Workflow

## Commit Route

このリポジトリでは、コード変更後のGit運用を次の流れで進める。

1. Codex が実装や修正を行う
2. 変更内容の要点と差分をユーザーに共有する
3. ユーザーが差分を確認する
4. ユーザーから明示的にOKが出たらコミットする

## Rules

- ユーザーのOKが出る前にコミットしない
- 必要に応じて `git diff` の内容を共有する
- ブランチ作成や `git push` も、必要なら事前確認を取る
- 差分確認までは実装のみ進め、Git操作は保留する
